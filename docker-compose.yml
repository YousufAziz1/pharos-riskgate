version: '3.8'

services:
  riskgate:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - .:/workspace
    environment:
      - PYTHONUNBUFFERED=1
